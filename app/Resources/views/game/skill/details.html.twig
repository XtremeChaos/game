{% extends 'base.html.twig' %}

{% block body %}
    <a class="btn btn-success" href="{{ path('skill_list') }}">Inapoi La listare</a>
    <h2>Vizualizare Skill</h2>
    <hr>
    <h2 class="page-header">{{ skill.name }}</h2>

    <p>Class: {{ skill.className }}</p>
    <p>Type: {{ skill.description }}</p>


{% endblock %}